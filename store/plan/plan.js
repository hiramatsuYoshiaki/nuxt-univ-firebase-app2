export const state = () => ({
//   planList: [],
  plan: [
    'Firebaseでホスティングする。',
    'Circle CI でGitHubにpushしたら自動でディプロイする。',
    'dotenv を使って環境変数を設定し、Firebaseのconfigで使う。'
  ]
})

//   export const getters = {
//     get(state) {
//       return state.list
//     }
//   }
